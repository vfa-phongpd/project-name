export enum FILE_SIZE {
    FILE_SIZE_REQUIRE = 1024 * 1024
}

export enum TYPE_FILE {
    PNG = 'image/png',
    JPG = 'image/jpeg',
    JPEG = 'image/jpg'
}

export enum FILE {
    DISTINATION = './upload',
    UPLOAD_DISTINATION = './vouchers/',
    FILE_INTERCEPTOR = 'image',
    UPLOAD_INTERCEPTOR = 'uploaded_file'
}